---
title: "Blog | Prompt Consulting"
permalink: "/blog/index.html"
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Blog | Prompt Consulting</title>
  <meta name="description" content="Insights on accounting, audit, corporate tax, VAT and business setup in the UAE."/>
  <link rel="stylesheet" href="/blog/assets/blog.css"/>
</head>
<body>
  <header class="header">
    <a class="logo" href="/">Prompt Consulting</a>
    <nav class="nav">
      <a href="/">Home</a>
      <a href="/blog/">Blog</a>
    </nav>
  </header>
  <main class="container">
    <section class="hero">
      <h1>Insights &amp; Updates</h1>
      <p>Accounting, Audit &amp; UAE Tax updates to help you make better decisions.</p>
    </section>
    {% if collections.posts | length %}
    <section class="grid">
      {% for post in collections.posts | reverse %}
        <article class="card">
          <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
          <p>{{ post.data.description }}</p>
          <div class="meta">
            {% if post.data.category %}<span class="pill">{{ post.data.category }}</span>{% endif %}
            {% if post.date %}<time datetime="{{ post.date | isoDate }}">{{ post.date | readableDate }}</time>{% endif %}
          </div>
        </article>
      {% endfor %}
    </section>
    {% else %}
    <section class="empty-state">
      <h2>Posts coming soon</h2>
      <p>We&apos;re preparing fresh guidance on UAE tax, audit, and accounting. Check back shortly.</p>
    </section>
    {% endif %}
  </main>
  <footer class="footer">
    © <span id="year"></span> Prompt Consulting · <a href="/">Back to Home</a>
  </footer>
  <script>document.getElementById('year').textContent=new Date().getFullYear()</script>
</body>
</html>
